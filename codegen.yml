overwrite: true
schema: src/graphql/generatedSchema.graphql
documents: "src/**/*.graphql"
generates:
  src/generated/graphql-request.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
  ./graphql.schema.json:
    plugins:
      - "introspection"
